using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UpdateHazardData : MonoBehaviour
{

    // TODO have dictionary as a field ??


    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    // When a marker is moved and released, update the appropriate dictionary
    public void UpdateHazardPosition(/*the current marker whose position we want to update?*/)
    {

        // Get the current marker's new position using transform.position

        // Update the marker's position in its dictionary

    }
}
